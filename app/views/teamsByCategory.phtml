<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Equipos por categoría</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

<div class="container">

    <div class="header">
        <h1 class="logo">PuntoCopa</h1>
        <h2 class="subtitulo">Equipos de <?= htmlspecialchars($pais) ?></h2>
    </div>

    <div class="nav">
        <a href="index.php?action=categorias">Volver a categorías</a>
        <a href="index.php?action=home">Inicio</a>
    </div>

    <div class="fixture">
        <?php foreach ($teams as $team): ?>
            <div class="partido">
                <div class="team">
                    <span><?= htmlspecialchars($team->nombre) ?></span>
                    <small>Categoría: <?= htmlspecialchars($team->pais) ?></small>
                </div>

                <div class="resultado">
                    🏆 <?= htmlspecialchars($team->copas_libertadores) ?>
                </div>
            </div>
        <?php endforeach; ?>
    </div>

</div>

</body>
</html>